# Developer environment: editable install with test/lint tooling.
# Version and pip dependencies are defined in pyproject.toml.
name: pypharao-dev
channels:
  - conda-forge
dependencies:
  - python>=3.10
  - numpy>=1.22
  - rdkit>=2022.09
  - tqdm>=4.66
  - pip
  - pip:
    - -e ".[dev]"
